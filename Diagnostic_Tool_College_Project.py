
print("Welcome to our diagnostics tools \n")
error = (input("What is the issue you are facing? \n")).lower()
print("\n")

synonyms = ["application","app","software","applcation","applicaton","applcaton"]

def mainFunc():
    x=0
    while x < len(synonyms)
        if synonyms in error and "loading" in error or "load" in error and "not" in error or "wont" or "won't":
            cableConnected = (input("Are you connected to your wifi? \n")).lower()
            if cableConnected == "yes":
                print("test")
            elif cableConnected == "no":
                print("Connect to your wifi")
    x+=1

mainFunc()
